review docs/performance.md then use a venv to run scripts/unified_benchmark.py to test ['float32', 'float64', 'float16', 'int8', 'int16', 'int32', 'int64', 'uint8', 'uint16', 'uint32', 'uint64', 'complex64', 'complex128', 'turboquant'] for dimenstions 128,384,768 for counts 500,1000,3000,7000,15000,25000,50000  ingestion and ALL search types (all hybrid and other vectorsearch, geo-spatial, graphrag, and temporal searches) for both CPU and Metal on this host, then ssh to ancalagon and run the same tests for CPU CUDA on amd64 linux. test both hosts in parallel. allocate 18 gb to longbow for testing, and collect pprof data and monitor logs for errors. use 30s timeouts for small batches, and upto 60 seconds for large ones, and then update docs/performance.md with different results listed for each host, when done with latest results, and then provide executive summary comparing to previous docs/performance.md results, with suggestions for performance improvements added to the end of docs/nextsteps.md as "suggested improvments"

